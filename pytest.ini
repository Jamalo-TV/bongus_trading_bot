[pytest]
testpaths = tests
pythonpath = .
addopts = --strict-config --strict-markers
required_plugins =
    pytest-asyncio>=1.4
    pytest-trio>=0.8
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
